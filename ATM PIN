#include <stdio.h>

int main()
{
    int pin;

    printf("Enter ATM PIN: ");
    scanf("%d", &pin);

    if(pin == 1234)
        printf("Access Granted");
    else
        printf("Wrong PIN");

    return 0;
}
